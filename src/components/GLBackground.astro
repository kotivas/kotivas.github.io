<canvas id="gl-canvas"></canvas>

<script>
  import { initScene } from "../gl/scene";

  const canvas = document.getElementById("gl-canvas") as HTMLCanvasElement;
  initScene(canvas);
</script>

<style>
  canvas {
    position: fixed;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
    pointer-events: none;
    z-index: 0;
  }
</style>
