<section class="hero">
  <div class="hero-identity">
    <div class="hero-nickname">kotivas</div>
    <div class="hero-name">Ivan</div>
  </div>
  <div class="hero-description">
    C++ developer exploring the intersection of software, graphics, and
    mathematics through real-time systems and visual experiments.
  </div>
  <div class="hero-buttons">
    <a href="#project" class="btn-nav">projects</a>
    <a href="#about" class="btn-nav">about</a>
    <a href="#contact" class="btn-nav">contact</a>
  </div>
</section>

<style>
  .hero {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    overflow: hidden;
    padding-top: 10rem;
    padding-bottom: 23rem;
    gap: 2.5rem;
  }

  .hero-identity {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .hero-nickname {
    letter-spacing: -0.05em;
    font-weight: 600;
    font-size: clamp(3.5rem, 10vw, 8rem);
    line-height: 1;
    color: var(--color-primary);
  }

  .hero-name {
    font-weight: 300;
    font-size: 1.75rem;
    color: var(--color-muted);
  }

  .hero-description {
    padding: 0rem 2rem;
    color: var(--color-muted);
    font-weight: 350;
    font-size: 1rem;
    line-height: 1.5;
    max-width: 450px;
  }

  .hero-buttons {
    display: flex;
    gap: 2.5rem;
  }

  .btn-nav {
    font-weight: 400;
    letter-spacing: 0.05em;

    border-radius: 999px;
    text-decoration: none;
    color: var(--color-muted);
    transition: transform 0.2s ease;

    transition:
      transform 0.25s cubic-bezier(0.2, 0.8, 0.2, 1),
      box-shadow 0.25s ease;
  }

  .btn-nav:hover {
    transform: translateY(-2px);
  }

  .btn-nav:active {
    transform: translateY(0) scale(0.97);
  }
</style>
