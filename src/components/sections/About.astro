<section id="about" class="about">
  <div class="about-container">
    <h2 class="about-title">About</h2>
    <div class="section-card">
      <div class="about-text">
        <div class="about-col">
          <p class="about-text-first">
            I'm a self-taught C++ developer focused on real-time graphics and
            low-level programming.
          </p>
          <p>
            My core stack is C/C++, Vulkan, OpenGL, GLSL and HLSL. I've worked
            with raymarching, SDF rendering, and built my own small rendering
            engine.
          </p>
          <p>
            I also build desktop and web applications, including tools in Tauri,
            low-latency C++ applications with Asio and design-focused websites.
          </p>
          <p class="about-text-last">
            I balance studies with daily coding and am open to freelance work
            and new opportunities.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-title {
    display: block;
    font-size: clamp(3rem, 5vw, 4rem);
    font-weight: 500;
    color: var(--color-primary);
    margin: 0 0 2.5rem;
  }

  .about {
    padding: clamp(1rem, 4vw, 4rem);
    max-width: 1350px;
    margin-inline: auto;
  }

  .about-container {
    padding-bottom: 3rem;
  }

  .about-text {
    display: flex;
    gap: 2rem;
    color: var(--color-primary);
  }

  .about-col {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .about-text p {
    line-height: 1.5;
    font-size: 1.5rem;
    color: color-mix(in srgb, var(--color-primary) 90%, transparent);
    margin: 0;
    max-width: 1028px;
  }

  @media (max-width: 900px) {
    .about-text {
      grid-template-columns: 1fr;
    }

    .about-text p {
      font-size: 1.3rem;
    }
  }

  .about-text-first,
  .about-text-last {
    font-size: 1.75rem;
    font-weight: 600;
  }
</style>
